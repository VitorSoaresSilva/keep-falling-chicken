namespace Utilities
{
    public static class Enums
    {
        public enum SaveGames
        {
            PlayerData,
            ConfigData,
        }

        public enum Audios
        {
            Effects,
            Music
        }

        public enum SceneIndexes
        {
            Manager,
            LevelOne,
            Boss,
            LevelTwo,
        }
    }
}
